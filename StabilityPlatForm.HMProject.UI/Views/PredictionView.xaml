<UserControl x:Class="StabilityPlatForm.HMProject.UI.Views.PredictionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Margin="0,10,0,0">
        <Border Background="White" CornerRadius="12">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="5" Color="#000000" />
            </Border.Effect>
        </Border>
        <Border Padding="40" Background="White" CornerRadius="12" RenderOptions.ClearTypeHint="Enabled">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Margin="0,0,0,20" HorizontalAlignment="Center" FontSize="60" Text="🤖" />
                <TextBlock HorizontalAlignment="Center" FontSize="28" FontWeight="Bold" Foreground="#333333" Text="AI Stability Prediction Model" />
                <TextBlock Margin="0,15,0,0" HorizontalAlignment="Center" FontSize="16" Foreground="#888888" Text="钙钛矿寿命预测大模型模块，即将上线..." />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>